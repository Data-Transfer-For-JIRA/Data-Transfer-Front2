import { Box } from '@mui/system'

type props = {
  handleDarkMode: () => void
}
export default function PageHeader({ handleDarkMode }: props) {
  return (
    <header>
      <Box>

      </Box>
    </header>
  )
}
